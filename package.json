{
  "name": "home-buyer-intelligence-engine",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered Dutch home buyer document analysis and bidding advice",
  "scripts": {
    "dev": "pnpm run --parallel /dev/",
    "dev:frontend": "pnpm --filter frontend run dev",
    "dev:backend": "cd apps/backend && uv run uvicorn src.main:app --reload --port 8000",
    "build:frontend": "pnpm --filter frontend run build",
    "lint": "nx run-many --target=lint",
    "generate:types": "bash infra/scripts/generate-types.sh"
  },
  "devDependencies": {
    "nx": "^20.4.0",
    "@nx/vite": "^20.4.0",
    "@nx/eslint": "^20.4.0",
    "prettier": "^3.4.0"
  }
}
